#pragma once

// Config for compilation options like wireless, number of axies, etc

namespace config {
    // Wireless
    const char *ssid = "TP-Link_7A50";
    const char *password = "89741238";
    const int port = 80;
}